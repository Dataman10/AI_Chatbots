[project]
name = "genai-series"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Hitesh", email = "hiteshdataman@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "langchain>=1.3.15",
]

[project.scripts]
genai-series = "genai_series:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
